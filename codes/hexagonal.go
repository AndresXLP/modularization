└─ src
	└─ cmd
		└─ api (grpc/cli...)
			├─ handler
			└─ router
		└─ providers (Inyeccion de Dependencias)
		main.go
	├─ config (environments)
	├─ docs*
	└─ core
		├─ app (logica de negocio)
		├─ ports (Interfaces de comunicacion)
		├─ domain
		└─ adapters
	├─  mocks*
	└─ utils/pkg*